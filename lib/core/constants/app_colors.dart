import 'dart:ui';

import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color appPrimary = Color(0xFF1A3263);    // Navy Deep (Authority/Ocean)
  static const Color appSecondary = Color(0xFF547792);  // Steel Blue (Industrial/Reliable)
  static const Color appAccent = Color(0xFFFD5E53);     // Safety Gold (Action/Visibility)

// UI Colors
  static const Color appScaffold = Color(0xFFF0F2F5);   // Cloud Grey (Clean Background)
  static const Color appSurface = Colors.white;         // Card/Form backgrounds
  static const Color appTextColor = Color(0xFF0D1B2A);  // Deep Slate (High contrast text)
  static const Color appTextMuted = Color(0xFF4A5766);  // Muted Steel (Label text)
  static const Color appErrorColor = Color(0xFFD32F2F); // Safety Red (Alerts)
  static const Color appBorderColor = Color(0xFFB0BEC5); // Metal Grey (Field borders)
}
